# Miscellaneous

- `artifacts/` — retained logs and legacy output files
- `docs/` — development notes that do not define the current architecture
- `scripts/` — general workspace/toolchain utilities

Core RTL, model code, FPGA synthesis, and ASIC visualization files do not
belong in this directory.
